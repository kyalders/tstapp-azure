terraform {
  required_providers {
    azurerm = {
      source  = "hashicorp/azurerm"
      version = "=2.46.0"
    }
  }
}
# Configure the Microsoft Azure Provider (azurerm provider) in provider blocks.
provider "azurerm" {
  features {}
}